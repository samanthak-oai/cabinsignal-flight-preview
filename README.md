# CabinSignal sample preview

This repository hosts a static CabinSignal interface preview using fixed sample data.
It contains no flight data credentials, live provider integration, query collection, or
tracking. The production extension remains gated on flight data licensing and the
documented feasibility review.
